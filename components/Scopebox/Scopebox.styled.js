import styled from "styled-components";

export const StyledScopebox = styled.article`
  position: relative;
  border: 1px solid var(--scopebox-border-color);
  padding: 10px;
  margin-top: 10px;
  background-color: #ffffff;
`;
